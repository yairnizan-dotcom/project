import streamlit as st

# --- כותרת ראשית של האפליקציה ---
st.title("🎬 CinePredict AI")
st.info("🤖 מערכת בינה מלאכותית מתקדמת לחיזוי והמלצות סרטים")

# --- חלק הקלט מהמשתמש ---
st.header("📝  הזנת פרטי הסרט")

movie_name = st.text_input("מה שם הסרט שלך?")
user_prompt = st.text_area("הזינו את תקציר עלילת הסרט (כולל ז'אנר, שחקנים וסיפור):")

# --- תנאי להצגת התוצאות ---
if user_prompt:
    
    # סימולציה של תשובת המודל
    response = "הסרט בעל פוטנציאל הצלחה גבוה מאוד! המודל צופה ציון של 8.5 ותגובות חיוביות מהקהל."
    
    # --- חלק הפלט והתוצאות ---
    st.header("📊 שלב 2: תוצאות הניתוח")
    
    st.success("✅ הניתוח הושלם בהצלחה!")
    
    st.subheader("💡 תוצאת החיזוי עבור הסרט:")
    st.write(response)
    
    # הבלטת מידע חשוב נוסף בעזרת צבע אחר
    st.warning("⚠️ המלצת המודל: מומלץ להפיץ את הסרט במהלך חופשת הקיץ כדי למקסם רווחים.")
